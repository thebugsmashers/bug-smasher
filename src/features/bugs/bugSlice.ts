// import { createAsyncThunk, createSlice, PayloadAction } from '@reduxjs/toolkit';
// import { RootState } from '../../app/store';

// export const bugSlice = createSlice({
//   name: 'bug',
//   initialState,
//   reducers: {
//     smash: (state: BugState, id) => {
//       console.log('STATE: ', state.bugID);
//       console.log('SMASH BEFORE: ', state.isSmashed);
//       console.log('SMASH');
//       document.getElementById(id);
//       state.isSmashed = true;
//       console.log('SMASH AFTER: ', state.isSmashed);
//     },
//   },
// });

// export const selectBug = (state: RootState) => state.bugs.isSmashed;

// export const { smash } = bugSlice.actions;
// export default bugSlice.reducer;
export {};
